<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Insta profile</title>
    <link rel="stylesheet" href="/instagram.css">
</head>
<body>
    <h2>this profile belongs to <%= data.name %></h2>
    <button>Follow</button>
    <button>Message</button>
    <p>Followers: <%= data.followers%> &nbsp;&nbsp;&nbsp; Following: <%= data.following%> %></p>
    <hr>
    <%for(let post of data.posts){ %>
        <div class="post-container">
        <img src="<%= post.image %>" alt="cat img">
        <p>Likes: <%= post.likes%> &nbsp;&nbsp;; Comments: <%= post.comments%> %></p>
        </div>
    <% } %>
    <%- include("includes/footer") %><!-- used as sub-templates so we dont need to write the particular code again and again -->
</body>
</html> 